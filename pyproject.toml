[project]
name = "vast-ai-provisioning"
version = "0.1.0"
description = "Vast.ai provisioning helpers for vLLM from R2"
requires-python = ">=3.10"
dependencies = [
  "vastai>=1.0.9",
]

[tool.uv]
package = false

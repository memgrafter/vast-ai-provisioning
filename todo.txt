# TODO

- [ ] Expose SSH by default for all Vast vLLM templates and retrofit existing remote templates:
  - Add `use_ssh: true` to `config/templates/vllm-r2-base.public.json`; `ssh_direct: true` is already present but rendered/remote templates can still show `use_ssh: false` when omitted.
  - Confirm whether Vast requires `runtype` changes or explicit `22` port mapping for this template family; current evidence suggests the missing `use_ssh` flag is the likely gap.
  - Add launcher support to attach a local public key after `create_instance` when configured, following the llama.cpp launcher's `create_ssh_key` / `attach_ssh` path.
  - Generate a dedicated local key, e.g. `~/.ssh/vast_ai_provisioning_ed25519`, and never commit the private key. Public key can be distributed to Vast/account/template as needed.
  - Retrofit active remote templates by re-rendering from launch profiles and applying `--hash-id` updates, then verify new instances expose either `ssh_host`/`ssh_port` or `ports["22/tcp"]`.

- [ ] Add native fallback support for Vast instances whose direct mapped ports are present in `show_instance().ports` but unreachable externally:
  - Parse Cloudflare quick-tunnel URLs from Vast logs, especially the `Default Tunnel started for vLLM API (http://localhost:8000) - https://...trycloudflare.com` line.
  - Probe direct Vast mapped port first (`ports["8000/tcp"][0].HostPort`), then fallback to the tunnel URL when direct TCP/connect times out.
  - Register the selected reachable base URL in `~/.pi/agent/models.json` and burn-in scripts.
  - Note: Cloudflare quick-tunnel path currently shows significant inter-tool-call latency for coding-agent loops; treat tunnel fallback as functional but undesirable for sustained benchmark runs.
  - Keep direct-port resolution via Vast API; do not guess random ports. Use remote `lsof` only to debug internal container listeners (for example `localhost:18000` vs portal/proxy `localhost:8000`), not to discover the external HostPort.
  - Also fix local model-ui readiness checks to include the vLLM API key; repeated local `GET /v1/models` 401s can make the UI timeout even when vLLM is actually healthy.

- [ ] P0: Use Vast's better verification strategy during offer selection.
  - Do not rely only on the SDK `verified` boolean; recent RTX PRO 6000 searches returned `verified=false` even when `verification` was `verified`.
  - Classify hosts from `verification` (`verified` / `unverified` / `deverified`) plus `is_vm_deverified` and `vericode`.
  - Update `offer_passes_policy` / `require_verified` handling to accept explicit `verification == "verified"` when appropriate and reject explicit deverified hosts unless a profile intentionally allows them.
  - Print the verification classification in offer policy output so selection decisions are auditable.

- Add a pre-launch guard that verifies required Vast account-level runtime env vars are actually available to launched instances:
  - `AWS_ACCESS_KEY_ID`
  - `AWS_SECRET_ACCESS_KEY`
  - `VLLM_API_KEY`
- Do not print secret values. Only report present/missing and value length if needed.
- Prefer a cheap diagnostic/provisioning mode or marker env check before renting GPU time for full model sync.
- If Vast SDK cannot inspect account env directly, add safe early provisioning diagnostics and have the monitor terminate immediately on missing required env.

- Greylist by reason so we can consider those machines more carefully. We may want to collect a little more data, maybe as JSONL per greylist entry in `greylist.jsonl`.

- Build a preferred-machine book by controlled instance churn:
  - Launch short-lived interruptible probes to classify machines by Docker cache/startup speed, R2 speed, provisioning success, vLLM startup, and outbid behavior.
  - Investigate whether Vast search can include currently unavailable/rented machines. If "unavailable" simply means currently rented, we may be able to map the full machine universe instead of only currently rentable machines.
  - Use this book to prefer known-good machines and avoid wasting time on unknown/slow machines.
  - Move preferred machine IDs out of launch profiles into a dedicated JSON config organized by GPU name and GPU count, so known-good preferences can be reused across generated/compiled launch templates.

- Add a SQLite history database for launch/offer/outcome decisions:
  - Record every searched offer, selected offer, machine ID, GPU, advertised disk/network/cost, market type, bid cap, launch timestamp, and final outcome.
  - Record measured facts: image cached or not, time to provisioning, R2 speed test MB/s, sync duration, vLLM ready time, API success, outbid/stopped time, and failure reason.
  - Capture broad rental metadata before/during/after runs, including Vast instance/offer JSON, host/GPU/runtime details, vLLM config/metrics, benchmark inputs/results, logs, and any available hardware telemetry, so we can audit performance and detect bad rentals later.
  - Use the DB for scoring machines over time instead of relying only on static preferred/greylisted arrays.
  - Derive decision quality metrics: success rate, expected useful minutes, effective cost per useful hour, launch-time percentage, and interruption probability by machine/GPU/market.

- Add optional profile-driven single-file transfer later only if needed.

- Harden template manifest safety:
  - `scripts/apply_vast_template.py` currently only checks that the rendered filename is listed in `state/templates/manifest.json`; consider also verifying payload metadata matches manifest fields (`template_name`, `model_profile_name`, `private`) before remote create/update.

- Review public template portal auth placeholder:
  - `config/templates/vllm-r2-base.public.json` contains `OPEN_BUTTON_TOKEN=1`; it is not the vLLM API key, but it is token-shaped public template data. Confirm this remains intentional or replace with a safer placeholder/private overlay value.

- Add spot-vs-on-demand savings guard for interruptible launches:
  - Treat submitted `spot.max_bid_dph` as the effective possible compute price, because Vast running instance pricing appeared to use the submitted bid (`dph_base=0.9`) rather than the lower listed interruptible price.
  - Add env var `VAST_MIN_SPOT_SAVINGS_RATIO` defaulting to `0.20`.
  - For interruptible profiles, compare `(max_bid_dph + storage)` to matching on-demand total for the same offer/machine/GPU when available.
  - If spot-at-bid is not at least `VAST_MIN_SPOT_SAVINGS_RATIO` cheaper than on-demand, warn and prefer/fallback to on-demand or reject with `insufficient_spot_savings`.
  - Print listed spot dph, submitted bid dph, spot total at bid, on-demand total, savings %, threshold %, and decision before launch.

- Benchmark cache diagnostics: add per-step /metrics deltas for prompt_total/local_compute/local_cache_hit and compute token_cache_share; add prefix identity assertion; add single-user fixed-bucket probe (e.g. 1 user x 3 turns x 90k) to verify exact prefix caching before multi-user runs.

- [ ] Add geo-tiered Vast offer search for unverified RTX 5090 tests: try US first, then NA (`georegion=true geolocation=NA`), then privacy-friendly western Europe + JP (`geolocation in [GB,IE,FR,DE,NL,BE,CH,AT,DK,SE,NO,FI,ES,PT,IT,JP]`), then broad fallback. Keep `search_no_default: true`, `min_inet_down >= 1000 Mbps`, and the provisioner R2 speed test (`R2_SPEED_TEST_MIN_MBPS=100`) enabled.

- [ ] Run all Vast vLLM profiles hotter by default: update all model profiles/templates to `gpu_memory_utilization=0.93` where appropriate, not just RTX 5090. Treat 0.93 as the operating target across headless/datacenter-style hosts; if startup, CUDA graph, OOM, or runtime stability issues appear, back off per-profile and document the failure.

- [ ] Add a TurboQuant KV-cache profile path for long-context RTX 5090 agentic coding:
  - TurboQuant is a vLLM KV-cache quantization/compression path selected through `--kv-cache-dtype`, not a model-weight quantization/re-export path.
  - Start with Carnice NVFP4 MTP n=3 on RTX 5090, `gpu_memory_utilization=0.93`, `max_num_seqs=1`, and `kv_cache_dtype=turboquant_k8v4`.
  - First target `max_model_len=49152`; check startup logs for `GPU KV cache size`, MTP activation, and any TurboQuant/attention errors.
  - If stable, try `turboquant_4bit_nc` around 65536 context. Only try more aggressive presets after quality/acceptance checks.
  - Keep non-TurboQuant 16K/18K profiles available as rollback baselines.
  - Do not stream/re-export models for TurboQuant; it should be a profile/template setting only.

- [ ] Add calibrated KV-cache quantization/scaling checks per model/profile, especially every profile using `kv_cache_dtype=fp8`:
  - Qwen3.6-27B-FP8 B200/H200/PRO6000WS profiles currently rely on vLLM default/unscaled FP8 KV factors; logs warn this can affect accuracy.
  - Carnice NVFP4/modelopt profiles need the same review when fp8 KV is enabled; weight NVFP4 calibration does not automatically imply KV-cache scaling is calibrated.
  - Qwen3.6 AWQ/AWQ-Marlin profiles need the same review when fp8 KV is enabled; weight AWQ calibration does not automatically imply KV-cache scaling is calibrated.
  - Decide whether to source scaling factors from checkpoint metadata, run calibration, or keep fp8 KV as throughput-only with documented quality caveat.

- B200 Carnice NVFP4 MTP3 peak-TPS notes, shared-prefix coding burn-in, max_tokens=1024 unless noted:
  - c=2: ~198 tps/user, 397 tok/s total. no queueing, ~2.6% KV, shared 60K prefix, 2 measured turns/user with 2048 output cap.
  - c=24: ~105 tps/user, 2528 tok/s total. no meaningful queueing, ~15% KV, shared 60K prefix, 2 measured turns/user with 2048 output cap.
  - c=128: ~29.6 tps/user, 3782 tok/s total. about 12 queueing at peak, ~76% KV, shared 60K prefix, 2 measured turns/user with 1024 output cap.
  - c=192: ~16.0 tps/user, 3071 tok/s total. about 33 queueing, ~100% KV / KV-limited, shared 60K prefix, 2 measured turns/user with 1024 output cap.
  - c=24 agentic sustained: ~118.5 tps/user, 2845 tok/s total. 4 sequential measured requests/user after 1 warmup, max_tokens=20000, shared 30K prefix (~30.5K prompt), max_waiting=7, max_kv=19.8%, latency avg/p95 ~76.9s/130.5s, TTFT avg ~0.70s. Recent windows held ~3.1k-3.55k tok/s at 20-24 running with ~83-86% draft acceptance.
  - c=64 agentic sustained: ~77.2 tps/user, 4942 tok/s total. 4 sequential measured requests/user after 1 warmup, max_tokens=20000, shared 30K prefix (~30.5K prompt), max_waiting=12, max_kv=50.3%, latency avg/p95 ~129.7s/216.9s, TTFT avg ~1.22s. Recent windows held ~4.7k-5.7k tok/s at 52-60 running with ~82-88% draft acceptance.
  - c=128 agentic sustained: ~42.6 tps/user, 5450 tok/s total. 4 sequential measured requests/user after 1 warmup, max_tokens=20000, shared 30K prefix (~30.5K prompt), max_waiting=26, max_kv=100.0%, latency avg/p95 ~241.8s/417.2s, TTFT avg ~14.56s, queue avg ~13.02s. Saturated upper point and now lower aggregate than c=96; recent high-throughput windows ~6.0k-6.5k tok/s at 92-103 running with ~83-87% draft acceptance.
  - c=96 agentic sustained: ~58.1 tps/user, 5575 tok/s total. 4 sequential measured requests/user after 1 warmup, max_tokens=20000, shared 30K prefix (~30.5K prompt), max_waiting=2, max_kv=75.3%, latency avg/p95 ~173.9s/295.0s, TTFT avg ~1.34s, queue avg ~0.11s. Best current sweet spot: higher aggregate than c=128 without KV saturation; recent windows ~5.4k-5.9k tok/s at 71-85 running with ~81-87% draft acceptance.
- Note: offer search currently needs a GPU-name query fix for profiles that set `allowed_gpu_names` without `preferred_gpu_name`; otherwise Vast returns other GPU types and policy filters them client-side.
